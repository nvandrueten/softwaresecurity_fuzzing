graph test-1 {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y {
       a -- b -- c;
       a -- {x y};
       x -- y [w=-340282366920938463463374607431768211468.ù,len=927876210349392699];
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
};
}
       a -- b -- c;

